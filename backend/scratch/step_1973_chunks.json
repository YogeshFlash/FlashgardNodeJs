[{"AllowMultiple":false,"EndLine":2319,"ReplacementContent":"    return result;\n  }\n\n  // ─── MobileAppCuts → MachineCutLog ────────────────────────────────────────\n  // Legacy columns expected:\n  //   MobileAppCutsID, AppUniqueId, OwnerID (record creator user legacy ID),\n  //   LicenseKey (hex-encoded key linking to a MobileUser which belongs to a retailer org),\n  //   ModelID, QRCode, Instruction, PlotterID, Latitude, Longitude,\n  //   IsPositiveCut, Reviews, CreatedDate\n  async migrateMobileAppCuts(\n    mobileCutsFile: Express.Multer.File | any[],\n    sourceName?: string,\n    mapsCache?: {\n      orgLegacyMap: Map<string, string>;\n      userLegacyMap: Map<string, string>;\n      modelLegacyMap: Map<number, string>;\n      licenseMap: Map<string, string>; // rawLicenseKey -> orgId (retailer/dealer)\n    }\n  ) {\n    let importedLogs = 0;\n    let skippedRows = 0;\n    const failures: any[] = [];\n\n    if (!mobileCutsFile) {\n      throw new Error('MobileAppCuts file is required');\n    }\n\n    const rows = Array.isArray(mobileCutsFile)\n      ? mobileCutsFile\n      : await this.parseCsvBuffer((mobileCutsFile as Express.Multer.File).buffer);\n\n    // ── Build lookup maps ──────────────────────────────────────────────────\n    // 1. org map: legacyId -> UUID\n    const orgs = mapsCache?.orgLegacyMap ??\n      new Map((await this.prisma.organization.findMany({\n        where: { legacyId: { not: null } },\n        select: { id: true, legacyId: true }\n      })).map(o => [o.legacyId!, o.id]));\n    const orgLegacyMap: Map<string, string> = orgs instanceof Map ? orgs : new Map((orgs as any[]).map((o: any) => [o.legacyId!, o.id]));\n\n    // 2. user map: legacyId -> UUID (for OwnerId = record creator)\n    const users = mapsCache?.userLegacyMap ??\n      new Map((await this.prisma.user.findMany({\n        where:
<truncated 11509 bytes>