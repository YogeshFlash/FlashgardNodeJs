"        const legacyIdStr = String(row.MobileAppCutsID || row.MobileAppCutsId || row.Mobileappcutsid || row.Id || row.id || row.ID || '').trim();\n        const appUniqueId = String(row.AppUniqueId || row.AppUniqueID || row.Appuniqueid || row.AppuniqueId || row.app_unique_id || '').trim() || null;\n\n        // OwnerId = record owner / creator user\n        const ownerLegacyId = String(row.OwnerID || row.OwnerId || row.OwnerUserId || row.CreatedByUserId || row.owner_id || row.ownerid || '').trim();\n        const userId = userLegacyMap.get(ownerLegacyId) ?? null;\n\n        // Organization = the org the record creator belongs to\n        const orgLegacyId = String(row.OrganizationID || row.OrganizationId || row.OrgID || row.OrgId || row.org_id || row.orgid || '').trim();\n        const organizationId = orgLegacyMap.get(orgLegacyId) ?? null;\n\n        // DealerId: resolve from LicenseKey via the MobileUser's license → org\n        const rawLicenseKey = String(row.LicenseKey || row.Licensekey || row.license_key || row.licensekey || '').trim();\n        const dealerId = resolveDealerFromLicenseKey(rawLicenseKey) ?? null;\n\n        // Model\n        const modelLegacyIdNum = parseInt(String(row.ModelID || row.ModelId || row.model_id || row.modelid || '').trim());\n        const modelId = modelLegacyMap.get(modelLegacyIdNum) ?? null;\n\n        const rawDate = row.CreatedDate || row.CreatedAt || row.Createddate || row.Createddateutc || row.CreatedDateUtc || row.created_date || row.createdDate || row.createddate;\n        const createdAt = this.safeDate(rawDate);\n        if (!createdAt || createdAt.getFullYear() !== 2026) {\n          skippedRows++;\n          this.mobileAppCutsState.skipped++;\n          continue;\n        }\n\n        const logData: any = {\n          id: this.parseOrCreateUuid(`mobile-cut-${legacyIdStr}`),\n          appUniqueId: appUniqueId?.substring(0, 255) || null,\n          organizationId,\n          userId,\n          dealerId,\n          modelId,\n          legacyLicenseKey: rawLicenseK
<truncated 833 bytes>