"    // ── Process rows ──────────────────────────────────────────────────────\n    const totalCount = rows.length;\n    for (const row of rows) {\n      if (this.mobileAppCutsState.shouldStop) {\n        this.logger.warn('MobileAppCuts migration stopped by user request.');\n        break;\n      }\n      this.mobileAppCutsState.processed++;\n      this.mobileAppCutsState.message = `Processing row ${this.mobileAppCutsState.processed} of ${totalCount}...`;\n\n      try {\n        const legacyIdStr = String(row.MobileAppCutsID || row.Id || '').trim();\n        const appUniqueId = String(row.AppUniqueId || row.AppUniqueID || '').trim() || null;\n\n        // OwnerId = record owner / creator user\n        const ownerLegacyId = String(row.OwnerID || row.OwnerUserId || row.CreatedByUserId || '').trim();\n        const userId = userLegacyMap.get(ownerLegacyId) ?? null;\n\n        // Organization = the org the record creator belongs to\n        const orgLegacyId = String(row.OrganizationID || row.OrgID || '').trim();\n        const organizationId = orgLegacyMap.get(orgLegacyId) ?? null;\n\n        // DealerId: resolve from LicenseKey via the MobileUser's license → org\n        const rawLicenseKey = String(row.LicenseKey || '').trim();\n        const dealerId = resolveDealerFromLicenseKey(rawLicenseKey) ?? null;\n\n        // Model\n        const modelLegacyIdNum = parseInt(String(row.ModelID || '').trim());\n        const modelId = modelLegacyMap.get(modelLegacyIdNum) ?? null;\n\n        const createdAt = this.safeDate(row.CreatedDate || row.CreatedAt);\n        if (!createdAt || createdAt.getFullYear() !== 2026) {\n          skippedRows++;\n          this.mobileAppCutsState.skipped++;\n          continue;\n        }\n\n        const logData: any = {\n          id: this.parseOrCreateUuid(`mobile-cut-${legacyIdStr}`),\n          appUniqueId: appUniqueId?.substring(0, 255) || null,\n          organizationId,\n 
<truncated 3167 bytes>